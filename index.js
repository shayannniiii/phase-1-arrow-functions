const divide = function(a, b) {
    return a / b;
  }
  const square = (x) => {
    return x * x;
  }
  const add = (a, b) => {
    return a + b;
  }
  const adding = (a, b) => a + b;

module.exports = { add };